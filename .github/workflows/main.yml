name: NexCache CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y build-essential liblz4-dev libzstd-dev libssl-dev pkg-config

    - name: Build NexCache
      run: |
        # Usiamo il Makefile originale SENZA modifiche ambientali. 
        # Il codice ora è in grado di auto-dichiararsi hardware-safe.
        make clean
        make all

    - name: Run Tests
      run: |
        ./build/test_arena
        ./build/test_core_v2
        ./build/test_v4
